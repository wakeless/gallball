<%= simple_form_for(@game) do |f| %>
  <%= f.association :sport, collection: Sport.all %>
  <%= f.association :winner, :as => :select, :collection => Player.all %>
  <%= f.association :loser, :as => :select, :collection => Player.all %>
  <%= f.button :submit, :class => "btn-primary" %> 
<% end %>
