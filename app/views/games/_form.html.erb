<%= simple_form_for(@game) do |f| %>
  <%= f.input :winner_id, :as => :select, :collection => Player.all %>
  <%= f.input :loser_id, :as => :select, :collection => Player.all %>
  <%= f.button :submit, :class => "btn-primary" %> 
<% end %>
